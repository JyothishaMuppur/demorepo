pipeline {
    agent {label 'agentlinux'}
    
    stages { 

        stage {
            
        stage('checkversion') {
            steps{
                
                sh "node.. version"
                sh "npm ..version"
        }
    }
}
}
  
